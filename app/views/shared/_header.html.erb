<nav class="navbar navbar-fixed-top navbar-inverse" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Lurebook', root_path, :class => "navbar-brand"%>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse navbar-ex1-collapse">
      <ul class="nav navbar-nav">
        <li><%= link_to( '<span class="glyphicon glyphicon-book"></span> '.html_safe << 'ルアーカタログ',  lures_path ) %></li>
        <% if current_user %>
            <li><%= link_to( '<span class="glyphicon glyphicon-briefcase"></span> '.html_safe << 'myルアーボックス', {:controller => 'mylure' }) %></li>
        <% end %>
        <li><%= link_to( '<span class="glyphicon glyphicon-copyright-mark"></span> '.html_safe << 'メーカー',  makers_path) %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
            <li><%= link_to( '<span class="glyphicon glyphicon-user"></span> '.html_safe << current_user.name, user_path(current_user), :class => 'signOut') %></li>
<!--            <li><%= link_to( '<span class="glyphicon glyphicon-user"></span> '.html_safe << "ヘルプ", user_path(current_user), :class => 'signOut') %></li>　-->
            <li><%= link_to('ログアウト', [ :destroy, :user_session ], :method => :delete, :class => 'signOut') %></li>
        <% else %>
        <li><%= link_to('ログイン', [ :new, :user_session ], :class => "singIn") %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container -->
  
  


</nav>

